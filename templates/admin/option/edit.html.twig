{% extends 'admin/base.html.twig' %}

{% block title %}{{ parent() }} Editer l'option{% endblock %}

{% block body %}

    <div class="container mt-4">
        <div class="jumbotron">
        <h2>Editer le bien "{{ option.name }}"</h2>
        {{ include ('admin/option/_form.html.twig', {form: form, button: 'Editer'}) }}
{% endblock %}